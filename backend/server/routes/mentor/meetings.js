const router = require("express").Router();


// The meetings page
router.get("/mentor/:mentorid/meetings", (req, res) => {
    
});

// Meeting requests
router.get("/mentor/:mentorid/meetings/reqs", (req, res) =>{

});

// Currently scheduled meetings
router.get("/mentor/:mentorid/meetings/schedule", (req, res) =>{
    
});



router.post("/mentor/:mentorid/meetings", (req, res) => {
    
});